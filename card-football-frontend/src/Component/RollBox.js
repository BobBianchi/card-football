export default function RollBox({result}) {
    return (
        <div
            style={{
                border: "1px solid black",
                height: "100px",
            }}
        >
            {result}
        </div>
    )
}